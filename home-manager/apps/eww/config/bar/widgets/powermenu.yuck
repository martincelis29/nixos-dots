;; =================== POWERMENU WIDGET =====================
(defwidget power []
  (eventbox
    :cursor "pointer"
    :class "power"
    (button
      :class "icon"
      :tooltip "Powermenu"
      :valign "center"
      :onclick "${EWW_CMD} close player notifications calendar & ${EWW_CMD} open-many --toggle background-closer powermenu"
      ""
    )
  )
)